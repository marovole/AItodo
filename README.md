<p align="center">
  <img width="180" src="./public/ChatGPT.png" alt="AI Todo">
  <h1 align="center">AI Todo</h1>
  <p align="center">智能 AI 调研驱动的 Todo 桌面应用</p>
  <p align="center">Intelligent AI Research-Powered Todo Desktop Application</p>
</p>

<p align="center">
  <a href="#功能特性">功能特性</a> •
  <a href="#快速开始">快速开始</a> •
  <a href="#技术架构">技术架构</a> •
  <a href="#开发指南">开发指南</a> •
  <a href="#路线图">路线图</a>
</p>

---

## 项目简介

**AI Todo** 是一款创新的桌面 Todo 应用，将传统任务管理与 AI 深度调研能力相结合。

### 核心理念

> **从「记住要做什么」到「理解这个任务究竟需要什么」**

传统 Todo 应用假设用户已经清楚任务的具体内容，而 AI Todo 认识到：**很多任务在执行前需要先进行调研才能变成可执行的行动项**。

### 典型使用场景

- **投资研究**: 添加"调研 XX 公司的财务状况" → AI 自动生成详细调研报告
- **技术选型**: 添加"对比 React vs Vue 在大型项目中的表现" → 获得全面的技术分析
- **市场分析**: 添加"2024年新能源汽车市场趋势" → 收到结构化的市场洞察
- **学习计划**: 添加"Rust 语言学习路径" → 得到个性化的学习建议

---

## 功能特性

### 核心功能

| 功能 | 描述 | 状态 |
|------|------|------|
| **Todo 管理** | 创建、编辑、删除任务，支持状态流转 | ✅ 已完成 |
| **三栏布局** | 侧边栏导航 + 任务列表 + 详情面板 | ✅ 已完成 |
| **AI 自动调研** | 一键触发 ChatGPT Deep Research | ✅ 已完成 |
| **实时进度** | 调研过程实时可视化，支持查看实时画面 | ✅ 已完成 |
| **Markdown 渲染** | 调研结果以富文本格式展示 | ✅ 已完成 |
| **系统通知** | 调研完成时推送系统通知 | ✅ 已完成 |
| **数据持久化** | SQLite 本地存储，数据安全可靠 | ✅ 已完成 |

### 任务状态流转

```
待调研 (Pending) → 调研中 (Researching) → 待审阅 (Review) → 已完成 (Done)
      ↓                    ↓
   可直接完成          可取消/重试
```

### 界面预览

```
┌──────────────────────────────────────────────────────────────────┐
│                         AI Todo                                  │
├────────────┬─────────────────────────────┬───────────────────────┤
│            │                             │                       │
│  侧边栏     │         任务列表            │      详情面板          │
│  (200px)   │        (flex: 1)            │      (360px)         │
│            │                             │                       │
│ ┌────────┐ │  ┌───────────────────────┐  │  ┌─────────────────┐  │
│ │📥 待调研│ │  │ ○ 调研公司财务状况  🔄 │  │  │ 任务详情        │  │
│ │🔍 调研中│ │  │ ○ 对比技术方案      ⏳ │  │  │ ────────────── │  │
│ │📋 待审阅│ │  ├───────────────────────┤  │  │ 状态: 🔄 调研中 │  │
│ │✅ 已完成│ │  │ ○ 市场趋势分析      ⭐ │  │  │                 │  │
│ │────────│ │  └───────────────────────┘  │  │ 📊 调研结果     │  │
│ │⚙️ 设置  │ │                             │  │ (Markdown)      │  │
│ └────────┘ │  ┌───────────────────────┐  │  │                 │  │
│            │  │ ＋ 新建调研任务        │  │  │ [👁️ 查看实时]  │  │
│            │  └───────────────────────┘  │  └─────────────────┘  │
└────────────┴─────────────────────────────┴───────────────────────┘
```

---

## 快速开始

### 系统要求

- **操作系统**: macOS 10.15+, Windows 10+, Linux (Ubuntu 18.04+)
- **运行时**: 无需额外依赖，应用已打包所有必要组件
- **ChatGPT 账号**: 需要有效的 ChatGPT Plus 订阅（用于 Deep Research 功能）

### 安装方式

#### 方式一：下载预编译版本（推荐）

前往 [Releases](https://github.com/marovole/AItodo/releases) 页面下载对应平台的安装包：

| 平台 | 文件 | 说明 |
|------|------|------|
| macOS (Intel) | `AI-Todo_x.x.x_x64.dmg` | Intel 芯片 Mac |
| macOS (Apple Silicon) | `AI-Todo_x.x.x_aarch64.dmg` | M1/M2/M3 芯片 Mac |
| Windows | `AI-Todo_x.x.x_x64-setup.exe` | Windows 10/11 |
| Linux | `AI-Todo_x.x.x_amd64.deb` | Debian/Ubuntu |

#### 方式二：从源码构建

```bash
# 克隆仓库
git clone https://github.com/marovole/AItodo.git
cd AItodo

# 安装依赖
pnpm install

# 开发模式运行
pnpm tauri dev

# 构建生产版本
pnpm tauri build
```

### 首次使用

1. **启动应用** - 双击打开 AI Todo
2. **登录 ChatGPT** - 首次使用需在内嵌浏览器中登录 ChatGPT 账号
3. **创建任务** - 在输入框中输入调研主题，如"分析 Tesla 2024 年财报"
4. **开始调研** - 点击任务后，在详情面板中点击"开始调研"按钮
5. **查看结果** - 调研完成后会收到系统通知，点击查看 Markdown 格式的调研报告

---

## 技术架构

### 系统架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                    AI Todo Desktop Application                      │
│                           (Tauri 2.0)                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    React Frontend Layer                      │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌──────────────┐ │   │
│  │  │ TodoSidebar│ │ TodoList  │ │TodoDetail │ │ResearchProgress│   │
│  │  └───────────┘ └───────────┘ └───────────┘ └──────────────┘ │   │
│  │                         ↓ Zustand Store                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              ↓ IPC                                  │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     Rust Core Layer                          │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │   │
│  │  │  cmd.rs  │  │ todo.rs  │  │ setup.rs │  │   db.rs   │    │   │
│  │  │ IPC命令   │  │ Todo管理 │  │ 窗口管理  │  │ 数据库    │    │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              ↓                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              Embedded WebView (ChatGPT)                      │   │
│  │  ├─ 自动注入 deep_research.js                                │   │
│  │  ├─ MutationObserver 监控 DOM 变化                           │   │
│  │  └─ IPC 事件通信 → Rust → React                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              ↓                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    SQLite Local Storage                      │   │
│  │  todos | research_results | settings | webview_sessions      │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **桌面框架** | Tauri 2.0 | 轻量级 (~10MB)，原生性能，Rust 安全 |
| **前端** | React 18 + TypeScript | 组件化 UI，类型安全 |
| **状态管理** | Zustand | 轻量、简洁的状态管理 |
| **样式** | Tailwind CSS | 实用优先的 CSS 框架 |
| **后端** | Rust | 高性能、内存安全 |
| **数据库** | SQLite + Rusqlite | 嵌入式数据库，零配置 |
| **WebView 自动化** | JavaScript 注入 + IPC | 绕过 API 限制，直接操作页面 |

### 项目结构

```
AItodo/
├── src/                          # React 前端源码
│   ├── view/
│   │   └── Todo/
│   │       ├── index.tsx         # Todo 主入口
│   │       └── components/       # Todo 相关组件
│   │           ├── TodoLayout.tsx
│   │           ├── TodoSidebar.tsx
│   │           ├── TodoList.tsx
│   │           ├── TodoItem.tsx
│   │           ├── TodoDetail.tsx
│   │           ├── TodoCreate.tsx
│   │           ├── ResearchProgress.tsx
│   │           ├── MarkdownViewer.tsx
│   │           └── Toast.tsx
│   ├── stores/
│   │   └── todoStore.ts          # Zustand 状态管理
│   ├── hooks/
│   │   └── useTodoContext.tsx    # Todo 上下文 Hook
│   ├── styles/
│   │   └── todo.css              # Todo 样式
│   └── types.d.ts                # TypeScript 类型定义
│
├── src-tauri/                    # Rust 后端源码
│   ├── src/
│   │   ├── main.rs               # 应用入口
│   │   └── core/
│   │       ├── mod.rs
│   │       ├── cmd.rs            # IPC 命令处理
│   │       ├── todo.rs           # Todo 数据管理
│   │       ├── db.rs             # SQLite 数据库
│   │       ├── setup.rs          # 窗口和 WebView 设置
│   │       └── window.rs         # 窗口管理
│   ├── scripts/
│   │   └── deep_research.js      # ChatGPT 自动化脚本
│   ├── Cargo.toml                # Rust 依赖配置
│   └── tauri.conf.json           # Tauri 配置
│
├── openspec/                     # OpenSpec 规范文档
│   ├── ai-todo-system-spec.md    # 系统规范
│   ├── project.md                # 项目约定
│   └── changes/                  # 变更提案
│
├── docs/                         # 文档
│   └── manual-testing-plan.md    # 手工测试计划
│
├── package.json                  # 前端依赖
├── vite.config.ts                # Vite 配置
├── tailwind.config.js            # Tailwind 配置
└── tsconfig.json                 # TypeScript 配置
```

### 数据模型

```sql
-- 核心 Todo 表
CREATE TABLE todos (
    id TEXT PRIMARY KEY,              -- UUID
    title TEXT NOT NULL,              -- 任务标题
    description TEXT,                 -- 描述
    url TEXT,                         -- 参考链接
    status TEXT DEFAULT 'pending',    -- pending/researching/review/done
    priority INTEGER DEFAULT 0,       -- 优先级
    research_result TEXT,             -- 调研结果 (Markdown)
    created_at DATETIME,
    updated_at DATETIME
);
```

---

## 开发指南

### 环境要求

```bash
# Rust (latest stable)
rustup update stable
rustc --version  # >= 1.70

# Node.js
node --version   # >= 18.0

# pnpm
pnpm --version   # >= 8.0

# Tauri CLI
cargo install tauri-cli
```

### 开发命令

```bash
# 安装依赖
pnpm install

# 开发模式（热重载）
pnpm tauri dev

# 类型检查
pnpm run build  # 仅前端
cargo check --manifest-path src-tauri/Cargo.toml  # 仅后端

# 构建生产版本
pnpm tauri build

# 清理构建缓存
cargo clean --manifest-path src-tauri/Cargo.toml
rm -rf dist node_modules/.vite
```

### 代码规范

- **Rust**: 使用 `rustfmt` 格式化
- **TypeScript**: 使用 Prettier 格式化
- **提交信息**: 遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范

```bash
# 格式化代码
cargo fmt --manifest-path src-tauri/Cargo.toml
pnpm prettier --write "src/**/*.{ts,tsx}"
```

### 调试技巧

1. **前端调试**: 开发模式下按 `Cmd+Option+I` (Mac) 或 `F12` (Windows) 打开 DevTools
2. **后端日志**: 设置 `RUST_LOG=debug` 环境变量查看 Rust 日志
3. **WebView 调试**: 在 ChatGPT WebView 中同样可以打开 DevTools

---

## 路线图

### MVP Phase 1 (✅ 已完成)

- [x] 项目基础设施搭建
- [x] SQLite 数据库集成
- [x] Todo CRUD 功能
- [x] 三栏布局 UI
- [x] ChatGPT Deep Research 自动化
- [x] 调研结果 Markdown 展示
- [x] 系统通知
- [x] 错误处理和边缘情况

### Phase 2 (计划中)

- [ ] 多 AI 服务支持 (Perplexity, Gemini)
- [ ] 调研结果对比功能
- [ ] 高级搜索和筛选
- [ ] 数据导出 (PDF, Markdown)
- [ ] 调研历史和分析

### Phase 3 (规划中)

- [ ] 浏览器扩展 (一键添加 Todo)
- [ ] 剪贴板监控
- [ ] 快捷键增强
- [ ] 性能优化
- [ ] 多语言支持

---

## 常见问题

### Q: 为什么需要登录 ChatGPT？

AI Todo 使用你的 ChatGPT 账号进行调研。我们不会存储你的账号密码，登录会话通过本地 Cookie 持久化。

### Q: 调研结果保存在哪里？

所有数据保存在本地 SQLite 数据库中，位于：
- macOS: `~/Library/Application Support/com.nofwl.chatgpt/`
- Windows: `%APPDATA%/com.nofwl.chatgpt/`
- Linux: `~/.local/share/com.nofwl.chatgpt/`

### Q: 如何更新应用？

目前需要手动下载新版本。自动更新功能正在开发中。

### Q: 支持哪些 AI 服务？

目前仅支持 ChatGPT (需要 Plus 订阅以使用 Deep Research)。Perplexity 和 Gemini 支持计划在 Phase 2 添加。

---

## 贡献指南

欢迎贡献代码、报告问题或提出建议！

1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'feat: add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request

---

## 致谢

- [lencx/ChatGPT](https://github.com/lencx/ChatGPT) - 本项目 Fork 自此优秀项目
- [Tauri](https://tauri.app/) - 现代桌面应用框架
- [Microsoft Fluent Design](https://www.microsoft.com/design/fluent/) - 设计灵感来源

---

## 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

<p align="center">
  <sub>Built with ❤️ by the AI Todo Team</sub>
</p>
